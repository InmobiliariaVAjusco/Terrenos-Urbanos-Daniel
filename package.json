
{
  "name": "inmuebles-v-static-site",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de compra-venta de inmuebles que utiliza React con un setup sin bundler (import maps). Este package.json es para compatibilidad con Vercel.",
  "scripts": {
    "build": "echo 'No se requiere paso de compilación. Vercel servirá los archivos estáticos directamente.'"
  },
  "dependencies": {},
  "devDependencies": {}
}
