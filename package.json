
{
  "name": "inmuebles-v-static-site",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma de compra-venta de inmuebles que utiliza React. Este package.json está configurado para transpilar TypeScript con esbuild para Vercel.",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/index.js --format=esm --external:react --external:react-dom/client --external:@google/genai && cp index.html dist/ && cp -r public/* dist/ 2>/dev/null || :"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
